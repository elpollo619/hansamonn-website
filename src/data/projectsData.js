// Re-export from legacy location for backward compatibility.
// New code should import from here: import { projectsData, categories } from '@/data/projectsData'
export { projectsData, categories } from '@/components/ProjectData';
